<%= form_for(@user_favorite) do |f| %>
  <% if @user_favorite.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_favorite.errors.count, "error") %> prohibited this user_favorite from being saved:</h2>

      <ul>
      <% @user_favorite.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :pet_id %><br>
    <%= f.number_field :pet_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
